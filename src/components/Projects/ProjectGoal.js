import classes from "./Goal.module.css";

const ProjectGoal = (props) => {
  return (
    <section className={classes.goal}>
      <h2>하루하루는 최선을 다해, 인생 전체는 되는대로</h2>
      <p>최선의 하루가 모이면, 괜찮은 삶이 될 것이다</p>
    </section>
  );
};

export default ProjectGoal;
